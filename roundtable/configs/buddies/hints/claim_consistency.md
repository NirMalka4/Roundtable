The claim's disposition and severity do not name a legal adjudication cell.

CORRECTIVE ACTION:
- Use `none` severity for rejected or not-applicable claims.
- Use `high` only for an upheld claim.
- Use `medium` or `low` for an upheld claim whose consequence is contained.
- For insufficient evidence, rate the worst outcome the evidence supports — `medium` when
  it could conceal a blocker, `low` otherwise. `high` is reserved for upheld claims.
- Correct the inconsistent field without deleting a supported claim.
