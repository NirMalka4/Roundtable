Return exactly one decision for every supplied eligible remediation and no others.
Match the Judge claim and primary reviewer finding ids exactly. Give a concise
material reason and at least one evidence citation. A publish decision requires
repository evidence independently corroborated by a successful `view` call whose
`view_range` exactly matches the cited lines. Draft citations must quote the exact
named component.
