One or more reviewer findings were left unadjudicated.

CORRECTIVE ACTION:
- Output one claim for EVERY reviewer finding delivered to you — including the ones you
  reject or find not applicable. Never silently drop or merge a finding to simplify the
  verdict.
- In each claim's `source_finding_ids`, cite the exact `agent::finding_id` of every
  finding that claim adjudicates, copied verbatim as delivered.
- The listed finding id(s) above have no claim citing them. Add the missing claim(s), or
  correct a mistyped id, until every delivered finding is covered.
- Do not invent an id to satisfy this check; cite only findings you were actually given.
