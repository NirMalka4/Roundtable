One or more claims cite evidence in a file that is not part of this change.

CORRECTIVE ACTION:
- Claim evidence is the grounding published with the claim, so cite the changed code a
  reader can open on this pull request.
- Correct the `file` (and line) to the actual in-change location you observed, or drop
  the citation if the claim does not rest on it.
- Keep a citation outside the change only when it is the verification that decides the
  claim, and say in `observation` what you checked there.
