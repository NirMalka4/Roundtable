Your output did not match the required JSON structure.

CORRECTIVE ACTION:
- Emit ONLY a single valid JSON object — no prose, no markdown code fences, no preamble.
- Include every required field with the correct type.
- Re-read the field-level errors below (location → problem) and fix each one.
- Arrays that must be non-empty need at least one well-formed element.
